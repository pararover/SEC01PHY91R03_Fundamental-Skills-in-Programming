\section*{Syllabus}

    \subsection*{\topicSubTitle: \topicTitle}
        Software
        installation and start-up; locating and launching the application, exploring the % Scilab
        graphical interface of the software: console, %SciNotes, 
        script editor,
        variable browser; understanding the workspace: current directory, command history, working directory settings; using built-in help and documentation features; basic command execution: arithmetic expressions, variable assignments, managing output display; data types and variables: numeric types (integers, floats), strings, booleans; variable naming conventions; operators and expressions: arithmetic, relational, logical operators; operator precedence and associativity; code structure and readability: using comments, breaking long expressions, line continuation symbols; saving and executing scripts, %: creating \scl{.sce} and \scl{.sci} files,
        running code from % SciNotes
        the script editor
        or the command window.